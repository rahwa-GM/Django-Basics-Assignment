### Running the Server

1. Navigate to the project directory:

   `cd portfolio_site`

2. Start the Django development server with the following command:

   `python manage.py runserver`

3. Open your browser and navigate to:

   `http://127.0.0.1:8000`
